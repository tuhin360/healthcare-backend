export const userSearchableFields: string[] = ["email"]; // only for search term
export const userFilterableFields: string[] = [
    "email",
    "role",
    "status",
    "searchTerm"
]; // for all filtering options
